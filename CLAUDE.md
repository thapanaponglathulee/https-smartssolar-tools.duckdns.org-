# Solar Tools Center — บริบทโปรเจกต์

ระบบภายในของ **บริษัท สมาร์ท เอ็น คอนซัลแทนท์ จำกัด** ใช้งานจริงที่ `https://smartssolar-tools.duckdns.org/`
พนักงาน 31 คน ส่วนใหญ่เป็นช่างภาคสนามที่เข้าใช้งานผ่านมือถือ

ระบบมี 2 โมดูล: **บุคคล** (เช็คอิน ลา เบี้ยเลี้ยง ปฏิทิน ใบเซอร์ Dashboard อนุมัติ) และ **เครื่องมือ** (ยืม-คืน สอบเทียบ)
งานรอบนี้ **แตะเฉพาะโมดูลบุคคล** ห้ามแก้โมดูลเครื่องมือโดยไม่ได้รับอนุญาต

---

## วิธีทำงานของโปรเจกต์นี้ — อ่านก่อนทุกอย่าง

**บริษัทไม่มีซอร์สโค้ดของระบบจริง** และจะไม่ได้มา งานที่ทำที่นี่จึงเป็นสองขั้น

1. **ขั้นพิสูจน์ข้อกำหนด** — Claude Code สร้าง **ต้นแบบ** ขึ้นมาจากข้อกำหนดในโฟลเดอร์นี้ ให้คนของบริษัทกดเล่นจริงจนมั่นใจว่าข้อกำหนดถูก
2. **ขั้นส่งมอบ** — ส่ง *ข้อกำหนดที่พิสูจน์แล้ว* + *ต้นแบบที่กดได้* ให้ผู้พัฒนาระบบจริงไปทำลงระบบจริง

> ต้นแบบไม่ใช่ระบบจริง และไม่ต้องพยายามทำให้เป็นระบบจริง
> **หน้าที่ของต้นแบบคือทำให้ข้อกำหนดผิดถูกเห็นได้ด้วยตา** ไม่ใช่เก็บข้อมูลของบริษัท
> ห้ามเอาข้อมูลพนักงานจริงใส่ต้นแบบ ใช้ข้อมูลสมมติเท่านั้น

ระบบจริงที่ `smartssolar-tools.duckdns.org` มี backend — เห็น endpoint `/api`, `/api/hr`, `/api/admin` จากการเฝ้าดู network (ตารางถัดไป) แต่เข้าไม่ถึงโค้ด จึงรู้แค่พฤติกรรมที่สังเกตจากหน้าจอเท่านั้น

---

## สภาพแวดล้อมของต้นแบบ — ตอบแล้ว 8 ก.ย. 2569

### ต้นแบบ (UAT) — ของที่ Claude Code ทำและแก้ได้

```
ภาษา / รันไทม์:        HTML + CSS + JavaScript ES5 ล้วน รันบนเบราว์เซอร์
                       Node ใช้เฉพาะสคริปต์ build — ไม่มีที่ไหนระบุเวอร์ชัน
เฟรมเวิร์ก:            ไม่มีเลย · ของภายนอกมีแค่ Google Fonts
ฐานข้อมูล:             ไม่มี — เก็บใน localStorage คีย์ ss-hr-uat-v2
คำสั่งติดตั้ง:          ไม่มีขั้นตอน ไม่มี dependency
คำสั่งรัน dev:          npx http-server -p 8080 .   → เปิด http://localhost:8080
คำสั่งรันเทสต์:         ไม่มีเทสต์ ไม่มี test runner ไม่มี CI
คำสั่ง lint / format:   ไม่มี
โครงสร้างไฟล์หลัก:      โค้ดต้นแบบทั้งหมดอยู่ใต้ prototype/
                       หน้าเว็บอยู่ใน prototype/index.html ไฟล์เดียว · ไม่มี API
                       ค่าตั้งต้นอยู่ที่ prototype/assets/js/masters.js
                       แก้ค่าได้จากหน้าตั้งค่าในตัวต้นแบบ (views-admin.js)
วิธี deploy:            คัดลอกทั้งโฟลเดอร์ prototype/ ไปวางใต้ web root
```

### ระบบจริง — เข้าไม่ถึงโค้ด

```
ภาษา / รันไทม์ / เฟรมเวิร์ก / ฐานข้อมูล / deploy:   ไม่รู้ และไม่ต้องเดา
รู้เท่าที่สังเกตได้:   single page · PWA · มี /api, /api/hr, /api/admin
```

ห้าม Claude Code แก้ระบบจริง ห้ามยิง API ของระบบจริง ทำงานในต้นแบบอย่างเดียว

### กติกาเฉพาะของต้นแบบ

1. **ต้นแบบต้องสลับคนได้ในเครื่องเดียว** — เพราะ localStorage แยกตามเบราว์เซอร์ ถ้าไม่มีตัวสลับ จะพิสูจน์เส้นทางอนุมัติไม่ได้เลย (พนักงานยื่นในเครื่องตัวเอง หัวหน้างานอีกเครื่องจะไม่เห็น)
   ให้มีแถบ **"กำลังใช้งานเป็น: …"** อยู่บนสุดตลอด เปลี่ยนคนได้ทันทีโดยไม่ต้องล็อกอิน ครอบคลุมทุกบทบาท พนักงาน · หัวหน้างาน · ฝ่ายบุคคล · แอดมิน · ผู้บริหาร
   แถบนี้ **มีเฉพาะในต้นแบบ** ห้ามเขียนลงข้อกำหนดที่ส่งให้ผู้พัฒนาระบบจริง
2. **ต้องมีปุ่มล้างข้อมูลและปุ่มโหลดข้อมูลตัวอย่าง** เพื่อให้เริ่มทดสอบใหม่ได้ในคลิกเดียว
3. **audit log และสิทธิ์ PDPA ในต้นแบบเป็นแค่การแสดงให้เห็น ไม่ใช่การบังคับ** — ต้นแบบทำหน้าที่พิสูจน์ว่า *หน้าจอและกติกาถูก* ส่วนการบังคับจริงเป็นงานของระบบจริง อย่าอ้างว่าผ่านเพราะต้นแบบทำได้
4. **ค่าที่เป็นนโยบายต้องอยู่ในหน้าตั้งค่า ไม่ใช่ในโค้ด** — ต้นแบบมีหน้าตั้งค่าที่แก้ค่าได้แล้ว ครอบคลุม 22 พารามิเตอร์ และมีผลกับการคำนวณย้อนหลังทันที · `prototype/assets/js/masters.js` เก็บเฉพาะ**ค่าตั้งต้นตอน seed** ซึ่งเป็นที่ที่มันควรอยู่ · ค่าที่เพิ่มใหม่ต้องเพิ่มเข้าหน้าตั้งค่าด้วยเสมอ
5. **ต้นแบบคือส่วนหนึ่งของการส่งมอบ** — ผู้พัฒนาระบบจริงจะได้ทั้งข้อกำหนดและต้นแบบ ดังนั้นหน้าจอ คำในปุ่ม และข้อความ error ต้องเป็นของจริงตามที่อยากได้ ไม่ใช่ของชั่วคราว

---

## สิ่งที่สังเกตได้จากระบบที่รันอยู่

- หน้าเว็บเป็น **single page** โหลดครั้งเดียวแล้วสลับแท็บด้วย JavaScript ไม่มีการเปลี่ยน URL
- เป็น PWA — มี `manifest.json` (หมายเหตุ: `static/icon-192.png` ตอบ 404 อยู่)
- ฟอนต์ IBM Plex Sans Thai / IBM Plex Mono จาก Google Fonts
- Endpoint ที่พบจากการเฝ้าดู network

| Endpoint | Method | ใช้ตอน |
|---|---|---|
| `/api?action=getProjects` | GET | โหลดรายชื่อโครงการสำหรับ dropdown ไซต์งาน **ยิงครั้งเดียวตอนเปิดหน้า** |
| `/api?action=getCategories` | GET | โหลดหมวดหมู่เครื่องมือ |
| `/api` | POST | คำสั่งทั่วไป |
| `/api/hr` | POST | ข้อมูลฝั่งบุคคล |
| `/api/admin` | POST | ข้อมูลฝั่ง Admin Panel |

> **บั๊กที่รู้แล้ว:** การกลับจาก Admin Panel มาหน้าหลักไม่มีการยิง API ใด ๆ เลย ทำให้ dropdown ไซต์งานยังเป็นข้อมูลเก่า ดูงาน CI-01

---

## คำศัพท์ในระบบ (สำคัญ — อย่าแปลหรือเปลี่ยนชื่อ)

| คำ | ความหมาย |
|---|---|
| **ไซต์งาน / โครงการ** | หน่วยงานที่ช่างไปทำ ใช้ผูกกับเช็คอิน เบี้ยเลี้ยง และรายงานต้นทุน |
| **เช็คอิน** | การลงเวลาเข้างานประจำวัน มีเฉพาะขาเข้า **ไม่มีเช็คเอาต์** |
| **ค่าข้าว** | เงินค่าอาหารที่ระบบ**คำนวณอัตโนมัติ**จากเช็คอิน ตามเรตของไซต์ ไม่ต้องยื่นเบิก |
| **เบี้ยเลี้ยง** | เงินที่พนักงาน**ยื่นเบิกเอง**และต้องผ่านการอนุมัติ คนละเส้นทางกับค่าข้าว |
| **ลาชดเชยวันทำงาน** | วันหยุดที่ได้จากการทำงานในวันหยุด และจากการแปลงชั่วโมง OT |
| **โควตาพิเศษ** | วันลาที่แอดมินเพิ่มให้เป็นครั้งคราวช่วงบริษัทไม่มีงาน แยกยอดจากโควตาประจำปี |
| **ปฏิทินทำงาน** | ตารางที่ระบุว่าแต่ละวันของปีเป็นวันทำงานหรือวันหยุด มีประเภทวัน 5 แบบ วันทำงานปกติ · เสาร์ทำงาน · เสาร์หยุด · วันหยุดบริษัท · วันอาทิตย์<br>**เสาร์ที่ทำงานให้แอดมินติ๊กเองรายปี ห้ามใช้สูตรเสาร์เว้นเสาร์** (ตัดสิน 2 ก.ย. 2569) |

---

## ขอบเขตของระบบนี้ — เคาะ 8 ก.ย. 2569

โมดูลบุคคลมีสิบเรื่อง **และไม่มีเรื่องไหนเป็นเรื่องของโครงการ** — เรียงตามลำดับการพึ่งพา ของที่อยู่บนต้องมีก่อน ของที่อยู่ล่างถึงจะทำได้

| # | เรื่อง | ความสามารถ | เมนู | ไฟล์ข้อกำหนด |
|---|---|---|---|---|
| 1 | **Employee Management** — ทะเบียนพนักงาน สถานะ บัญชีผู้ใช้ สิทธิ์ | M1 | Staff/Account | `spec-account.md` *(ข้อกำหนดหน้าจอ)* |
| 2 | **Organization & Position** — บริษัท/ฝ่าย/แผนก · ตำแหน่งงาน · Reporting Line · Manager/Supervisor · Organization Chart | M1 | Staff/Account | `spec-org.md` *(ข้อกำหนดหน้าจอ)* |
| 3 | **Authority & Approval** — ใครอนุมัติอะไร ผู้อนุมัติสำรอง การเตือนใบค้าง | M2 | อนุมัติ | `spec-approval.md` |
| 4 | **Work Calendar & Holidays** — ปฏิทินทำงานรายปี ประเภทวัน 5 แบบ | M4 | ปฏิทิน | `spec-calendar.md` |
| 5 | **Attendance & Work Location** — เช็คอิน ประเภทงาน ไซต์ ลักษณะการไป สถานะรายวัน | M3 | เช็คอิน | `spec-checkin.md` · `spec-checkin-dropdown.md` |
| 6 | **Extra Working Hours** — ทำงานวันหยุด ชั่วโมง OT แปลงเป็นวันลาชดเชย | M3 M5 | เช็คอิน (CI-08 CI-10) | `spec-checkin.md` |
| 7 | **Leave Management** — ประเภทการลา โควตา การยื่น การตัดวัน | M5 | ลา | `spec-leave.md` |
| 8 | **Expense / Site Allowance** — ค่าอาหารอัตโนมัติ เบี้ยเลี้ยงที่ยื่นเบิก รอบตัดจ่าย | M6 | เบี้ยเลี้ยง | `spec-allowance.md` |
| 9 | **Certificate & Employee Document** — ใบเซอร์ วันหมดอายุ เอกสารพนักงาน | M7 | ใบเซอร์ | `spec-cert.md` *(ข้อกำหนดหน้าจอ)* |
| 10 | **Dashboard & Reports** — การมองเห็นและรายงาน | M9 | Dashboard · ศูนย์รายงาน | `spec-dashboard.md` |

**ชั้นรองรับ ไม่ใช่หัวข้องาน แต่ทุกข้อพึ่ง** — บริการกลาง (M10): audit log · ที่เก็บไฟล์แนบ · ระบบแจ้งเตือน · หน้าตั้งค่า · รูปแบบวันที่ · **ยังไม่มีเจ้าของ**

**อยู่นอกเฟสนี้** — ทะเบียนโครงการและไซต์งาน (M8) ในส่วนที่เป็นการมอบหมายกำลังคนและรายงานคน-วัน · ดู `dev-handoff/docs/spec-site.md`

> รายการนี้เขียนใหม่ 8 ก.ย. 2569 จากเดิมที่มีเจ็ดข้อและข้ามลำดับ · เพิ่มข้อ 3 ข้อ 4 ข้อ 10 ซึ่งมีงานและไฟล์ข้อกำหนดอยู่แล้วแต่ตกหล่นจากรายการ
> **ลำดับนี้ผูกกับความสามารถ M1–M10 ใน `แผนที่งานโมดูลบุคคล` ไม่ใช่ระบบเลขใหม่**

| ใครถือ | อะไร |
|---|---|
| **โมดูลบุคคล (ระบบนี้)** | ข้อเท็จจริงรายคนรายวัน — คนนี้ วันนี้ อยู่ไซต์ไหน ประเภทงานอะไร สถานะอะไร ได้กี่มื้อ |
| **project management (คนละระบบ)** | คน-วันต่อไซต์ · ต้นทุนแรงงาน · ความคืบหน้า · แผนกำลังคน |
| **จุดส่งต่อ** | ส่งออกแถวรายวันรายคน ที่มีไซต์หลัก ไซต์ที่แวะ ประเภทงาน สถานะรายวัน เหตุผลไซต์หยุดงาน — **ไม่รวมยอด ไม่ตีความ ไม่มีตัวเงิน** |

> **ระบบนี้ไม่ออกรายงานคน-วัน แต่ต้องเก็บข้อมูลให้รวมยอดได้ถูกต้อง** ตามกติกาใน CI-25
> ข้อมูลกำลังถูกบันทึกทุกวัน ถ้าโครงสร้างผิดจะย้อนกลับไปแก้ไม่ได้

---

## กติกาที่ต้องยึดเสมอ

1. **ทุกค่าที่เป็นนโยบายบริษัทต้องตั้งค่าได้ ห้าม hardcode** — เวลาเข้างาน จำนวนวันลา อัตราเบี้ยเลี้ยง วันหมดอายุ
2. **ห้ามแสดงจำนวนเงินค่าอาหารหรือเบี้ยเลี้ยงรายไซต์ให้พนักงานทั่วไปเห็น** เป็นข้อกำหนดจากผู้บริหาร
3. **ระบบนี้ไม่เก็บข้อมูลเงินเดือนหรือค่าแรง** OT แปลงเป็นวันหยุดอย่างเดียว ไม่มีการคำนวณเป็นเงิน
4. **ห้ามลบข้อมูลจริง** ใช้การทำเครื่องหมายยกเลิกแทน เพื่อรักษาประวัติ
5. **ทุกการแก้ไขข้อมูลย้อนหลังต้องมี audit log** — ใคร เมื่อไร จากค่าอะไรเป็นค่าอะไร
6. **เอกสารสุขภาพเป็นข้อมูลอ่อนไหวตาม PDPA** — เปิดดูได้เฉพาะ **เจ้าตัว · หัวหน้างานที่เป็นผู้อนุมัติใบนั้น · ฝ่ายบุคคล** และต้องมี audit log ทุกครั้งที่เปิด<br>ผู้บริหารและแอดมินระบบ **ไม่เปิด** รายงานแสดงแค่ "ครบ/ไม่ครบ" (ตัดสิน 2 ก.ย. 2569)
7. **UI เป็นภาษาไทยทั้งหมด** ใช้คำเดิมที่ระบบใช้อยู่ อย่าเปลี่ยนคำศัพท์
8. **ออกแบบสำหรับมือถือก่อน** ผู้ใช้หลักคือช่างที่หน้างาน สัญญาณไม่ดี

---

## โครงสร้างโฟลเดอร์

```
CLAUDE.md              ← ไฟล์นี้ · อ่านก่อนเสมอ
dev-handoff/           ← ชุดส่งงาน ห้ามแก้ เป็นสำเนาจากโฟลเดอร์ HR ของบริษัท
  README.md
  docs/                ← ข้อกำหนดทั้งหมด สัญญากลาง บันทึกการตัดสินใจ เช็กลิสต์งาน
prototype/             ← โค้ดต้นแบบที่ Claude Code สร้างและแก้ได้
  index.html · assets/ · tools/
  docs/                ← เอกสารของตัวต้นแบบเอง (ใบตรวจ UAT · สแตก · โครงสร้างข้อมูล)
```

> `dev-handoff/docs/shared-things.md` และ `decisions.md` เป็น **สำเนา ณ 8 ก.ย. 2569**
> ต้นฉบับอยู่ในโฟลเดอร์ HR ของบริษัท ถ้าต้นฉบับเปลี่ยนต้องคัดลอกมาทับใหม่
> **ห้ามแก้สำเนาแล้วคิดว่าต้นฉบับเปลี่ยนตาม**

---

## ข้อกำหนดงาน

- `dev-handoff/docs/spec-checkin.md` — เมนูเช็คอิน (CI-01 … CI-24)
- `dev-handoff/docs/spec-checkin-dropdown.md` — ดรอปดาวน์ประเภทงานและลักษณะการไป (CI-14 … CI-21)
- `dev-handoff/docs/spec-leave.md` — เมนูลา (LV-01 … LV-13)
- `dev-handoff/docs/spec-calendar.md` — เมนูปฏิทิน (CAL-01 …)
- `dev-handoff/docs/spec-approval.md` — เมนูอนุมัติ
- `dev-handoff/docs/spec-allowance.md` — เมนูเบี้ยเลี้ยง
- `dev-handoff/docs/spec-dashboard.md` — หน้า Dashboard
- `dev-handoff/docs/tasks.md` — เช็กลิสต์เรียงลำดับที่ควรทำ

- `dev-handoff/docs/spec-site.md` — โครงการ/ไซต์งาน **(ไม่อยู่ในเฟสนี้ — เป็นบันทึกอย่างเดียว ห้ามสั่งงาน ห้ามสร้างหน้าจอ ห้ามใส่ลงต้นแบบ)**

- `dev-handoff/docs/spec-account.md` — Employee Management **(ข้อกำหนดหน้าจอ — สร้างหน้าจอก่อน แล้วใช้หน้าจอตรวจว่าข้อมูลอะไรขาด)**
- `dev-handoff/docs/spec-org.md` — Organization & Position **(ข้อกำหนดหน้าจอ)**
- `dev-handoff/docs/spec-cert.md` — Certificate & Employee Document **(ข้อกำหนดหน้าจอ)**

ยังไม่มีข้อกำหนด: `spec-report.md` (ศูนย์รายงาน)

> **ข้อกำหนดหน้าจอต่างจากข้อกำหนดงาน** — สามไฟล์ที่กำกับว่า *ข้อกำหนดหน้าจอ* บอกว่าหน้าตาต้องเป็นอย่างไรและมีสถานะอะไรบ้าง เพื่อให้สร้างขึ้นมากดเล่นแล้วเห็นว่าข้อมูลอะไรยังคิดไม่ถึง · ไม่ได้แทนที่ใบสั่งงานเดิม

ทุกงานมี **เกณฑ์ตรวจรับ** ระบุไว้ ให้ใช้เป็นฐานเขียนเทสต์และเป็นตัวตัดสินว่างานเสร็จหรือยัง

---

## สิ่งที่ห้ามทำโดยไม่ถาม

- เปลี่ยนโครงสร้างตารางฐานข้อมูลที่มีข้อมูลจริงอยู่แล้ว
- ลบโครงการ ลบพนักงาน หรือลบเช็คอิน
- แตะโมดูลเครื่องมือ
- เพิ่มการเก็บข้อมูลเงินเดือน ค่าแรง หรือเลขบัญชี
- **สร้างบัญชีทดสอบ บัญชีกลาง หรือบัญชีที่ไม่ใช่คนจริง ไว้ในทะเบียนพนักงานของระบบจริง** — ทุกบัญชีในระบบจริงต้องผูกกับคนจริงหนึ่งคน (ตัดสิน 8 ก.ย. 2569 หลังพบบัญชี #test) · ในต้นแบบใช้ชื่อสมมติได้ แต่ต้องดูออกชัดว่าเป็นข้อมูลสมมติ
- แตะระบบจริง ไม่ว่าจะแก้โค้ด ยิง API หรือคัดลอกข้อมูลจริงออกมา
