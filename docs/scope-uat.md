# สรุป Scope จากเอกสารข้อกำหนด — ก่อนเริ่มทำ Mockup UAT

ระบบ smartssolar-tools.duckdns.org · โมดูลบุคคล
สรุปจากเอกสาร 7 ไฟล์ที่ได้รับ · จัดทำ 3 ก.ย. 2569

---

## 1. สิ่งที่อยู่ในเอกสาร

| เอกสาร | รหัสงาน | จำนวน | สถานะ |
|---|---|---|---|
| `spec-checkin.md` | CI-01 … CI-14 | 14 | CI-02 ถูกยกเลิก → เหลือ 13 |
| `spec-checkin-dropdown.md` | CI-15 … CI-21 | 7 | ครบ |
| `spec-leave.md` | BR-01 … BR-12 · LV-01 … LV-13 | 25 | LV-08 ย้ายไป CAL-06 · LV-11 ลดขอบเขต |
| `spec-allowance.md` | ALW-01 … ALW-16 | 16 | ครบ |
| `spec-approval.md` | APV-01 … APV-14 | 14 | ครบ |
| `spec-calendar.md` | CAL-01 … CAL-06 | 6 | ครบ |
| `tasks.md` | DSH-01 … DSH-14 | 14 | **ไม่มีไฟล์สเปก** มีแต่ชื่อในเช็กลิสต์ |
| **รวม** | | **96 รหัส** | ใช้งานจริง ~93 |

นอกจากนี้ `tasks.md` มีงานฝ่ายบุคคลที่ไม่ใช่งานพัฒนาอีก 24 รายการ (ล้างข้อมูล ยืนยันตัวเลข ตรวจสอบกับหน่วยงานราชการ) — **ไม่อยู่ในขอบเขต mockup**

---

## 2. เอกสารที่ถูกอ้างถึงแต่ยังไม่ได้รับ

ทั้ง 4 รายการนี้กระทบการออกแบบโดยตรง ไม่ใช่แค่รายละเอียดปลีกย่อย

| เอกสาร | ถูกอ้างที่ไหน | กระทบอะไร |
|---|---|---|
| **`สัญญากลาง-โมดูลบุคคล`** | ทุกไฟล์ (ตาราง "ผลกระทบข้ามเมนู") | นิยามของกลาง **S1–S20** และจุดเชื่อม **J1–J7** — เป็นโครงสร้างข้อมูลกลางที่ทุกเมนูอ้าง ถ้าไม่มี ผมต้องเดาโครงสร้าง S5 (สถานะรายวัน) S15 (โควตา) S17 (อัตรา) S18 (รอบตัดจ่าย) เอง |
| **`spec-dashboard.md`** | `tasks.md` มี DSH-01…DSH-14 | ยังไม่รู้หน้าตาและกติกาของ Dashboard เลย มีแต่ชื่องาน |
| **สเปกใบเซอร์ / เครื่องมือ** | DSH-05 · CAL-01 (ชั้น "ใบเซอร์ของตัวเองใกล้หมดอายุ") | ปฏิทินชั้น "ของฉัน" ต้องดึงใบเซอร์มาแสดง |
| **หน้าจอระบบปัจจุบัน** | ทุกไฟล์พูดถึงของเดิม เช่น Admin Panel มี 6 เมนู | ถ้าต้องการให้ mockup ต่อยอดจากของเดิม ผมต้องเห็นของเดิมก่อน |

---

## 3. ข้อขัดแย้งที่เอกสาร "เคาะแล้ว" — ผมจะยึดตามนี้

ทุกข้อมีการแก้ทับในเอกสาร ผมยึดฉบับล่าสุดเสมอ

| เรื่อง | ของเดิม | ที่ใช้จริง |
|---|---|---|
| ลิสต์ไซต์ในหน้าเช็คอิน | CI-02 แสดงเฉพาะ "กำลังดำเนินการ" | **CI-16** กรองตามประเภทงาน (O&M ต้องเห็นโครงการที่ปิดแล้ว) |
| ปฏิทินทีม | LV-08 | **CAL-06** เป็นชั้นข้อมูลของปฏิทินเดียว |
| ใบลาค้าง | LV-11 ส่งต่ออัตโนมัติเมื่อเกิน 72 ชม. | **APV-07** ไม่ส่งต่อ คงค้างไว้กับคนเดิม + เตือนซ้ำ |
| ใบลาเลยวันลา | BR-12 ตกไปทันที | **APV-08** ค้างไว้ก่อน ตกไปเมื่อเกินเพดาน (ตั้งต้น 30 วัน) |
| ผู้อนุมัติเบี้ยเลี้ยง | คนเดียวกับผู้อนุมัติใบลา | **ฝ่ายบุคคล** (ALW/APV) · ของฝ่ายบุคคลเอง → ผู้บริหาร |
| วันหมดอายุพักร้อน/ลาชดเชย | 30 มิ.ย. → 12 เดือนรายวัน | **31 ธ.ค. ของปีถัดจากปีที่ได้มา** เก็บถังละปี ไม่ใช่ถังละวัน |
| ฝ่ายบุคคลเห็นใบลาไหม | "ไม่เห็นเลย" | **เห็นแบบอ่านอย่างเดียว** ไม่มีปุ่มตัดสิน ยกเว้นใบที่ตกมาแบบ fallback (J4) และคิว "รอตรวจสอบ" |
| ตัวคูณตามประเภทสถานที่ | มีคอลัมน์ coefficient | **ตัดออก** ทุกค่าเป็น 1 · ใช้ตารางประเภทงาน + จำนวนมื้อแทน |

---

## 4. ข้อขัดแย้งที่ยัง "ไม่ได้เคาะ" — ต้องตัดสินก่อนเขียนโค้ดจริง

### 4.1 ค่ากลางเบี้ยเลี้ยง 240 บาท/วัน — สองไฟล์สั่งตรงข้ามกัน  ✅ เคาะแล้ว

- `spec-checkin.md` ตารางพารามิเตอร์: *"ค่ากลางเบี้ยเลี้ยง 240 บาท/วัน — มีอยู่แล้ว ย้ายมารวมที่หน้าตั้งค่า"* (คือ **เก็บไว้**)
- `spec-allowance.md` ALW-07 + `tasks.md`: *"**ลบทิ้ง** หน่วยผิดตั้งแต่ต้น"* และทำเครื่องหมาย **ด่วน** เพราะถูกใช้เบิกจริงไปแล้วอย่างน้อย 2 ครั้ง

หน่วยไม่ตรงกันด้วย — ที่เหลือของระบบคิด **รายมื้อ** แต่ค่านี้เป็น **รายวัน**

> **ข้อสรุป 3 ก.ย. 2569 — ยึด ALW-07 ลบออก**
> ตัดออกจากการคำนวณทุกเส้นทาง · เก็บข้อมูลเดิมที่เคยเบิกไว้ ห้ามลบ · **ตัดแถวนี้ออกจากตารางพารามิเตอร์ของ `spec-checkin.md` ด้วย**
> งานที่ตามมา: ฝ่ายบุคคลต้องไล่ตรวจรายการที่เบิกด้วยค่า 240 ไปแล้ว ก่อนปิดรอบจ่ายรอบแรก (มีใน `tasks.md` แล้ว)

### 4.3 ตัวเลขที่เอกสารบอกเองว่ายังไม่เคาะ

`spec-leave.md` เขียนหัวเอกสารว่า *"พร้อมสั่งงาน (เหลือกำหนดตัวเลข 3 จุด)"* แต่ไม่ได้ระบุว่าจุดไหน จากการไล่อ่าน ผมเจอค่าที่ยังไม่มีตัวเลข 4 จุด — ขอให้ยืนยันว่า 3 จุดที่ว่าคือข้อไหน

| ค่า | อยู่ที่ | สถานะ |
|---|---|---|
| เพดานคนลาพร้อมกันต่อวัน | LV-09 / BR-10 | ยังไม่มีตัวเลข (ตัวอย่างในสเปกใช้ 2 คน) |
| เพดานจำนวนวันพักร้อนที่สะสมได้ | LV-12 | *"ตั้งเพดานได้"* แต่ไม่บอกค่า |
| จำนวนวันลากิจของบริษัท | ตาราง seed = 6 · `tasks.md` = "ต้องยืนยัน" | รอฝ่ายบุคคลยืนยัน |
| จำนวนวันพักร้อน | ตาราง seed = 8 · `tasks.md` = "ต้องยืนยัน" | รอฝ่ายบุคคลยืนยัน |

### 4.4 เกณฑ์เวลามาสาย — ยังไม่มีค่าผ่อนผัน

`DSH-02` สั่งให้ *"ตรวจและแก้เกณฑ์เวลามาสาย แล้วคำนวณย้อนหลังใหม่ทั้งหมด"* แต่ไม่มีที่ไหนบอกว่าผ่อนผันกี่นาที
สเปกมีแค่ *เวลาเข้างาน 08:30* → ตีความได้ว่า 08:31 = สาย
**ข้อเสนอ:** ทำเป็นพารามิเตอร์ `graceMinutes` ค่าตั้งต้น 0 แล้วให้ฝ่ายบุคคลเคาะ

---

## 5. กติกาข้ามเมนูที่ mockup ต้องไม่ทำผิด

ห้าข้อนี้เป็นกติกาที่ผิดแล้วเห็นทันทีตอน UAT

1. **CI-03 / ALW-13** — หน้าเช็คอิน **ห้ามมีตัวเลขเงิน จำนวนมื้อ อัตรา หรือคำว่า "เบี้ยเลี้ยง"** ในทุกจุดที่พนักงานเห็น · ปุ่มเบิกได้ แต่ห้ามมีจำนวนเงินบนปุ่ม
2. **CI-14** — สถานะรายวัน **คำนวณตอนอ่าน ไม่บันทึกทับตอนสิ้นวัน** · ลำดับตัดสิน: เช็คอิน → ใบลาอนุมัติ → ใบลารออนุมัติ → วันหยุดตามปฏิทิน → ยกเว้น → ขาดงาน
3. **BR-12** — แยก **ยอดใช้ไป** ออกจาก **ยอดจอง** · รออนุมัติ = ไม่ตัดโควตา แต่กันยอด · ปฏิเสธ = คืนเต็มทันที · ยกเลิกหลังอนุมัติ = **คืนเข้าถังเดิมที่ตัดมา**
4. **APV-03 / APV-05** — ห้ามอนุมัติของตัวเอง (เหนือทุกกติกา) · หัวหน้าไม่เห็นเบี้ยเลี้ยง · ฝ่ายบุคคลไม่มีปุ่มตัดสินใบลา **ทั้งใน UI และใน API**
5. **CAL-03 / LV-07** — พนักงานเห็นเฉพาะของตัวเอง · หัวหน้าเห็นชื่อลูกทีมแต่ไม่เห็นประเภทลา · เอกสารสุขภาพเปิดได้เฉพาะหัวหน้าผู้อนุมัติใบนั้นกับฝ่ายบุคคล และต้องลง audit log ทุกครั้งที่เปิด

---

## 6. ขอบเขตที่เสนอสำหรับ Mockup UAT

Mockup เป็น HTML/JS ทำงานในเบราว์เซอร์ ไม่มี backend — ใช้ตรวจ **หน้าจอ ลำดับงาน และกติกาการคำนวณ** ก่อนลงมือพัฒนาจริง

### รอบที่ 1 (เสนอให้ทำก่อน) — ตรงกับ "รอบที่ 1–2" ใน `tasks.md`

| หน้าจอ | ครอบคลุมรหัส |
|---|---|
| **เช็คอิน** (พนักงาน) | CI-03 · CI-05 · CI-11 · CI-15 · CI-16 · CI-17 · CI-18 · CI-19 · CI-20 + แถบ CAL-02 |
| **แก้ไข/ยกเลิกเช็คอิน** | CI-06 (แก้เองใน 12 ชม. · เกินแล้วยื่นขออนุมัติ · ไม่มีปุ่มลบ) |
| **ยื่นใบลา** | LV-03 · LV-06 · LV-07 + พรีวิวการตัดถังตาม BR-01 · เตือนเกินสิทธิ์ BR-02 · กันทับซ้อน BR-07 |
| **ใบลาของฉัน** | LV-04 · BR-12 (6 สถานะ + ยอดใช้ไป/ยอดจอง แยกบรรทัด) |
| **ปฏิทิน** | CAL-01 · CAL-02 · CAL-03 · CAL-05 |
| **คิวอนุมัติ** (หัวหน้า) | APV-04 (ตีกลับอัตโนมัติ) · APV-05 · APV-06 · BR-04 (ปุ่ม "รับทราบ" แทน "ปฏิเสธ") |
| **ทีมของฉัน** (หัวหน้า) | CAL-06 · CI-14 สถานะรายวันครบ 7 สถานะ |
| **ตั้งค่า** (แอดมิน) | CI-04 ปฏิทินรายปี · CI-07 ยกเว้นเช็คอิน · S19 ประเภทงาน · S20 ลักษณะการไปงาน · LV-01/LV-02 ประเภทลา · พารามิเตอร์ทั้งหมด |

### รอบที่ 2 (ทำต่อเมื่อรอบ 1 ผ่าน)

- CI-08 ลาชดเชยวันทำงาน · CI-09 วันหมดอายุ · CI-10 OT · CI-13 กะกลางคืน
- CAL-04 กิจกรรม HR · APV-01 ตารางอำนาจอนุมัติ
- เบี้ยเลี้ยง: ALW-02 · ALW-06 · ALW-08 · ALW-11 · APV-09

### สิ่งที่ mockup พิสูจน์ไม่ได้ — ต้องทดสอบบนระบบจริงเท่านั้น

| เกณฑ์ตรวจรับ | เหตุผล |
|---|---|
| APV-05 / ALW-13 *"ตรวจใน payload หลังบ้าน"* | mockup ไม่มีหลังบ้าน แสดงได้แค่ฝั่งหน้าจอ |
| CI-12 แจ้งเตือน 09:00 · LV-10 LINE · APV-14 | ต้องมีระบบส่งข้อความจริง |
| APV-13 *"บันทึกลบไม่ได้แม้แต่แอดมิน"* | เป็นเรื่องสิทธิ์ในฐานข้อมูล |
| CI-21 แปลงข้อมูลเช็คอินเดิม 3,300+ รายการ | ต้องรันกับข้อมูลจริง |
| CI-01 *"ดึงรายชื่อไซต์ใหม่ทุกครั้ง"* | เป็นบั๊กของการเรียก API จริง |
| LV-07 PDPA เปิดไฟล์ไม่ได้ข้ามบัญชี | ต้องมีการควบคุมสิทธิ์ฝั่งเซิร์ฟเวอร์ |

ผมจะใส่หมายเหตุกำกับไว้บนหน้าจอที่เกี่ยวข้อง เพื่อไม่ให้ผู้ทดสอบเข้าใจว่าผ่านแล้ว

---

## 7. สถานะปัจจุบันและสิ่งที่รออยู่

**สถานะ 3 ก.ย. 2569: พักการสร้าง mockup รอไฟล์ `สัญญากลาง-โมดูลบุคคล`**

### รอ (บล็อกการเริ่มงาน)

| รายการ | เหตุผลที่บล็อก |
|---|---|
| **`สัญญากลาง-โมดูลบุคคล`** (S1–S20 · J1–J7) | เป็นนิยามโครงสร้างข้อมูลกลางที่ทุกเมนูอ้าง ถ้าเริ่มก่อนแล้วนิยามไม่ตรง ต้องรื้อ S5 (สถานะรายวัน) S15 (โควตา) S17 (อัตราต่อมื้อ) S18 (รอบตัดจ่าย) ทั้งหมด |

### รออยู่ แต่ไม่บล็อก (ทำเป็นพารามิเตอร์ในหน้าตั้งค่าไว้ก่อนได้)

| รายการ | อยู่ที่ |
|---|---|
| ยืนยันว่า "ตัวเลข 3 จุด" ที่ `spec-leave.md` พูดถึงคือข้อไหน | ข้อ 4.3 |
| ค่าผ่อนผันเวลามาสาย (นาที) | ข้อ 4.4 |
| `spec-dashboard.md` | ข้อ 2 |
| สเปกใบเซอร์/เครื่องมือ (สำหรับปฏิทินชั้น "ของฉัน") | ข้อ 2 |
| ภาพหน้าจอระบบปัจจุบัน ถ้าต้องการให้ mockup หน้าตาใกล้ของเดิม | ข้อ 2 |

### บันทึกการตัดสินใจ

| วันที่ | เรื่อง | ข้อสรุป | ต้องแก้เอกสารตาม |
|---|---|---|---|
| 3 ก.ย. 2569 | ค่ากลางเบี้ยเลี้ยง 240 บาท/วัน | ลบออกตาม ALW-07 | `spec-checkin.md` ตารางพารามิเตอร์ |
| 3 ก.ย. 2569 | เบี้ยเลี้ยงวันที่ลาครึ่งวัน | ไม่ได้เลย ตาม ALW-10 | `spec-checkin.md` หัวข้อ CI-14 |
| 3 ก.ย. 2569 | ขอบเขต mockup รอบ 1 | ตามข้อ 6 · เริ่มเมื่อได้สัญญากลาง | — |
